# Legacy

`sync_rules_classic.yaml` adalah format Sync Rules LAMA (bucket_definitions).
Sudah digantikan oleh `../sync_streams.yaml` (Sync Streams edition 3) yang
dipakai dashboard saat ini. Disimpan hanya untuk arsip. Jangan dipakai.
